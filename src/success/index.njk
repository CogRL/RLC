---
layout: base.njk
title: Payment Successful - RLClarity.shop
description: Your payment was successful. We'll be in touch soon to schedule your session.
---

<!-- Success Hero Section -->
<section class="hero-gradient pattern-dots py-16 md:py-24">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="animate-on-scroll">
            <div class="w-20 h-20 bg-green-500 rounded-full flex items-center justify-center mx-auto mb-6">
                <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
            </div>
            <h1 class="text-4xl md:text-5xl font-display font-bold mb-6">
                Payment <span class="gradient-text">Successful!</span>
            </h1>
            <p class="text-xl text-gray-300 max-w-2xl mx-auto mb-8">
                Thank you for choosing RLClarity.shop! Your payment has been processed successfully.
            </p>
            <p id="order-id-display" class="text-lg text-cyber-400 font-mono mt-4"></p>
        </div>
    </div>
</section>

<!-- Next Steps Section -->
<section class="py-16 md:py-20 bg-slate-900">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="card card-glow p-8 md:p-12 animate-on-scroll text-center">
            <h2 class="text-3xl font-bold text-white mb-8">What Happens Next?</h2>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
                <div class="text-center">
                    <div class="w-16 h-16 bg-primary-gradient rounded-2xl flex items-center justify-center shadow-primary mx-auto mb-4">
                        <span class="text-2xl font-bold text-white">1</span>
                    </div>
                    <h3 class="text-lg font-semibold text-white mb-2">Confirmation</h3>
                    <p class="text-gray-400 text-sm">
                        You'll receive an email confirmation with your order details (including your Order ID) within the next few minutes.
                    </p>
                </div>
                
                <div class="text-center">
                    <div class="w-16 h-16 bg-primary-gradient rounded-2xl flex items-center justify-center shadow-primary mx-auto mb-4">
                        <span class="text-2xl font-bold text-white">2</span>
                    </div>
                    <h3 class="text-lg font-semibold text-white mb-2">Verify Your Order</h3>
                    <p class="text-gray-400 text-sm">
                        Please message me on Discord (afos3122) with your <strong class="text-cyber-400">Order ID</strong> from your confirmation email so I can verify your purchase and schedule your session.
                    </p>
                </div>
                
                <div class="text-center">
                    <div class="w-16 h-16 bg-primary-gradient rounded-2xl flex items-center justify-center shadow-primary mx-auto mb-4">
                        <span class="text-2xl font-bold text-white">3</span>
                    </div>
                    <h3 class="text-lg font-semibold text-white mb-2">Transform</h3>
                    <p class="text-gray-400 text-sm">
                        Get ready for your transformative psychological coaching experience!
                    </p>
                </div>
            </div>
            
            <div class="bg-slate-800 rounded-xl p-6 mb-8">
                <h3 class="text-xl font-semibold text-white mb-4">Contact Information</h3>
                <div class="text-gray-300 space-y-2">
                    <p><strong>Discord:</strong> afos3122</p>
                    <p><strong>Important:</strong> When messaging, please include your <strong class="text-cyber-400">Order ID</strong> for faster verification.</p>
                    <p><strong>Response Time:</strong> Within 24 hours after verification.</p>
                    <p><strong>Questions?</strong> Feel free to reach out anytime!</p>
                </div>
            </div>
            
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="/" class="btn btn-primary">
                    Return Home
                </a>
                <a href="/services/" class="btn btn-secondary">
                    View Services
                </a>
            </div>
        </div>
    </div>
</section>

<script>
// Optional: Extract session ID from URL for tracking
document.addEventListener('DOMContentLoaded', function() {
    const urlParams = new URLSearchParams(window.location.search);
    const sessionId = urlParams.get('session_id');
    const orderIdDisplay = document.getElementById('order-id-display');
    
    if (sessionId) {
        if(orderIdDisplay) {
            orderIdDisplay.textContent = 'Your Order ID: ' + sessionId;
        }
        // You could send this to analytics or your own tracking system
        console.log('Stripe session ID (Order ID):', sessionId);
        
        // Optional: Send to Google Analytics or other tracking
        if (typeof gtag !== 'undefined') {
            gtag('event', 'purchase', {
                'transaction_id': sessionId,
                'currency': 'USD'
            });
        }
    }
});
</script> 